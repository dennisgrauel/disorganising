name: Audio
icon: file-audio
preview: source

fields:
  source:
    type: files
    query: page.audio.template('audio')
    uploads: audio
    multiple: false
    width: 1/2
  caption:
    type: textarea
